export type GenreInputDTO = {
  name: string;
  description: string;
  isAdultContent: boolean;
};

export type GenreOutputDTO = {
  id: string;
  name: string;
  description: string;
  isAdultContent: boolean;
};
